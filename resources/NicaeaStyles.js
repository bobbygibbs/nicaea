import {
  StyleSheet,
} from 'react-native';

const NicaeaStyles = StyleSheet.create({

  // COMPONENT STYLES
  sectionContainer: {
    marginTop: 16,
    marginBottom: 16,
    paddingHorizontal: 24,
  },

  primaryButton: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 16,
    paddingVertical: 12,
    borderRadius: 6,
    elevation: 4,
  },

  // TEXT STYLES
  header3: {
    fontSize: 24,
    fontWeight: '700',
  },
  body: {
    fontSize: 18,
    fontWeight: '500',
  },

  // TEXT WEIGHTS
  superHeavy: {
    fontWeight: '700',
  }
});

export default NicaeaStyles;